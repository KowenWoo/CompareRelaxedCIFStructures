# CompareRelaxedCIFStructures
A python script to generate plots and a summary report of two directories of relaxed crystal structures (cif format).

Used to compare relaxations between M3GNet and DeepRelax on a set of novel candidate crystals but can be generalized for relaxation predicitions between any two sources.
